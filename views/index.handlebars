{{!-- 
     * Setup the `index.handlebars` to have the template that Handlebars can render onto.

     * Create a button in `index.handlebars` that will submit the user input into the database. --}}

<div class='title'>
<h1>Burger Time!</h1>
<img id='image' src="./assets/img/Cheeseburger.jpg" alt="burger">
</div>


<div class='notEaten'>
<h2>Burgers to eat!!!</h2>
<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{>burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>
</div>

<div class='eaten'>
<h2>Burgers already eaten.</h2>
<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{>burgers/burger2-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>
</div>

<div class='addBurger'>
<h2>Add a burger</h2>
<form class='create-form'>

<div class="form-group">
  <label for="bur">Name your burger:</label>
  <input type="text" id="bur" name="name">
  <button type='submit' id='burButton'>Submit</button>
</div>
</form>
</div>